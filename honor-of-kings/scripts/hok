#!/bin/bash
# Honor of Kings Build Recommendation System
# Usage: hok <hero_name> or hok --list or hok --stats

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

python3 "$SCRIPT_DIR/build-recommend.py" "$@"
